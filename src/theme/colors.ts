export interface ThemeColors {
  common: {
    empty: string;
    filled: string;
    secondary: string;
  };
}

export const colors: ThemeColors = {
  common: {
    empty: '#fff',
    filled: '#000',
    secondary: '#ccc',
  },
};
